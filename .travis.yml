language: java
jdk: oraclejdk8

cache:
  directories:
    - $HOME/.m2

script:
  - mvn -Dgit push -u origin master=pu_travis_integration_test verify

services:
   - mysql
   
before_install:
  - mysql -e 'CREATE DATABASE seed;'